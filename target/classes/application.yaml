## YAML Template.
---
spring:
  datasource:
    url: jdbc:postgresql://localhost/appet
    username: postgres
    password: Nacionalcampeon04.
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: appet768@gmail.com
    password: kjxk zkoo weqn iqfy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
